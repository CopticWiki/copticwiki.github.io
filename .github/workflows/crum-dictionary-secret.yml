name: Crum Secret

on:
  push:
  
jobs:
  my_job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run a script
        env:
          API_KEY: ${{ secrets.GOOGLE_SHEETS_API_KEY_CRUM_DICTIONARY }}
        run: echo "The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }} and your API Key is $API_KEY."
        
